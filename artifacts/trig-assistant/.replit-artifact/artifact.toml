kind = "web"
previewPath = "/"
title = "El Asistente Trigonométrico"
version = "1.0.0"
id = "artifacts/trig-assistant"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22623

[services.development]
run = "pnpm --filter @workspace/trig-assistant run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trig-assistant", "run", "build" ]
publicDir = "artifacts/trig-assistant/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22623"
BASE_PATH = "/"
